azure:
  credential:
    client-secret:
      client-id: <client-id>
      tenant-id: <tenant-id>
      secret: <secret>

micronaut:
  object-storage:
    azure:
      default:
        name: profile-pictures-container
        endpoint: https://my-account.blob.core.windows.net
